# Schema for the configuration files of the event_dashboard module.
event_dashboard.settings:
  type: config_object
  label: 'event_dashboard settings'
  mapping:
    example:
      type: string
      label: 'Example'
